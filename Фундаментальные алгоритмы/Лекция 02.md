21.03

Инкапсуляция - не инструмент для работы с данными (дополнить+пояснить)
(Вставить теорию про readonly)

const
readonly
static - определяет сущность на уровне типа. Статическое поле одна для всех экземпляров одного типа. 
Статический метод - для его выполнения не нужно создавать объект
Статический конструктор - для инициализации статических полей

## Типы .net

| структурные типы | ссылочные типы | Массивы<br>Array   |
| ---------------- | -------------- | ------------------ |
|                  | is             | Type[] a1;         |
|                  | as             | int[] aint;        |
|                  | obj us Type    | aint = new int[10] |
|                  | obj as Type    | ...                |
|                  |                | Array.Sort(aint)   |
все ссылочные типы данных  - наследники типа object.
```C#
Parse
TryParse

var n = int.Parse("12345");
int n1;
int.TryParse("12345", out int n);
```

Clear
CopyTo
Reverse
это все статические методы

Многомерные массивы:
```C#
int[ , ] b_int = new int[5, 6];
int [][] c_int;
c_int = new int[10][];
c_int[0] = new int[6];
c_int[0][2] = 10;
```

Все методы назначенные для изменения строки на уровне экземпляра объекта, не изменяют объект, а возвращают строку.

Свойства и методы string
Length - длина строки
Contains()
Insert
Remove
Replace
Split
Trim
ToLower
ToUpper

Динамические строки StringBuilder
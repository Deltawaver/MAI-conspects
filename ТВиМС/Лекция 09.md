10.04
Свойства номрировки:
1) Усл. норм.
   ```
   9.1 
   ```
   $Ф(y) = 1/(2\pi)^0.5 \int e ^ (-t^2/2) dt = 1/(2\pi)^0.5 \int e ^ (-t^2/2) dt+ 1/(2\pi)^0.5 \int_0 e ^ (-t^2/2) dt = Ф(0) + Ф_0(y)$
   $Ф_0 (y) = 1/(2\pi)^0.5 \int_0 e ^ (-t^2/2) dt$
   $Ф(y) = Ф_0(y) + 1/2$
2) Функция распределения
   ```
   9.2
   ```

```
   9.3 график зависимости F_\xi (x)  от x
```

```
   9.4 график зависимости Ф_0 (x)  от x
```

таблица значений лапласа
$Ф_0 (y) = 1/(2\pi)^0.5 \int_0 e ^ (-t^2/2) dt$
$Ф_0 (0.82) = 0.2938$
$Ф_0 (0.825) ~= 0.295$
$Ф_0 (0.83) = 0.2967$
$Ф_0 (-0.82) = -0.2938$
$Ф_0 (5) = 0.49999....7$
$Ф_0 (x) ~=~ 0.5$ если x > 5
Табличку распечатать

```
9.5 грвфик зависимости f_\xi(x) от x
```

3) Характеристическая функция
   $\xi^* = (\xi - m) / \sigma$, \xi ~ n(m, \sigma^2)
   $f_\xi (y) = |^d_dy \fi^(-1) (y) | f_\xi[\fi^(-1)(y)$
   $\fi(x) = (x - m)/\sigma, \fi^(-1)(y) = \sigma y + m$ => $d/dy fi^(-1) (y) = 0$, $f_\xifi^(-1)(y) = 1/(2\pi)^0.5 e ^ (-(sigma y (m - n)^2 / 2 pi = 1/ (2\pi)^0.5 * e^(-vsigma^2/2)$
   $f_(\xi^*) (y) = 1/(2\pi)^0.5 * e^(-y^2 / 2)$ => $\xi^* = N(0, 1)$
   $g_(\xi^*) = M[e^(it\xi^*)] = int $  
``` 
Фото 9.6 
```

```
фото 9.7
```

Тогда 
$c'*e^(-0^2/2) = 1$ => c' = 1 => $g_\xi^* (t)= e^(-t^2/2)$
\xi^* = (\xi - m)/sigma, \xi = sigma \xi^* + m
g_\xi(t) = M[e^(it\xi)] = M[e^(it(sigma \xi^* + m))] = M[e^(it sigma \x^*) * e^(itm)] = e^itm g_\xi^* (sigma t) = e^(itm) e ^(-t^sigma^2) / 2 => g_\xi(t) = e^(itm) e ^(-t^sigma^2) / 2

4) Числовая характеристика
5) Правило трех сигма
   Вычимслим вероятность отклонения случайно величины \xi от её мат ожидания
   P(|\xi - m| <= sigma) = P(-k sigma <= \xi - m <= k sigma) = P (m - ksigma <= \xi <= m + ksigma) = F_\xi (m + ksigma) - F_\xi (m - ksigma) = Ф_0 ((m + ksigma - m)/ sigma) + 1/2 - (Ф_0 ((m - ksigma - m) / sigma) + 1/2) = Ф_0 (k) - Ф_0 (-k) = 2Ф_0(k) = {0.6826 при  k = 1; 0.9544 при k = 2; 0.99773 ~= 1 при k = 3}
   Т.е. P(|\xi - m| <= 3 sigma) ~= 1
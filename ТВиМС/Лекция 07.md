27.03
Закон Бернулли является частным случаем биноминального распределения b_n = 1
$\xi ~ Be(p)$ Такая случайная величина называется характеристической. И моделирует появление или не появление события А в одном испытании
$P(\xi  = k) = p^k q^(1-k), k=0,1$
$P(\xi  = k) = C^k_n p^k q^(n-k)$ 

| $\xi$ | 0   | 1   |
| ----- | --- | --- |
| p     | q   | p   |
Свойства:
1) Условие нормировки
   q + p = 1
2) Функция распределения
   $F_\xi (x) = {0, x < 0; q, a \leq x < x; 1, x \geq 1}$
3) Характеристическая фугкция
   $g_x(t) = M[e^(it\xi)] = \sum^1_k=0 e^(itk)p^kq^(1-k) = \sum^1_k=0 (pe^(it)^k*q(1-k) = pe^(it) + q$
4) Числовая характеристика
   $\nu^1_\xi = 1/i * d/dt * g_\xi(t) = 1/i * pe^(it) |_t=0 = p => m_\xi = p$
   $\nu^2_\xi = 1/i^2 * d^2/dt^2 * g_\xi(t) = 1/i^2 * ipe^(it) |_t=0 = p => d_\xi = \nu62_\xi - m^2_\xi = p - p^2 = p(1-p)=pq$
Пункт 3. Закон Прассона
Опр.
$\xi$ ~ П(а), а > 0
если $|(\xi = k) = a^k / k! * e^(-a)$, k = 0, 1, 2, ..., n, ...

| $\xi$ | 0                 | 1                 | ... | n                 | ... |
| ----- | ----------------- | ----------------- | --- | ----------------- | --- |
| p     | $a^0/0! * e^(-a)$ | $a^1/1! * e^(-a)$ | ... | $a^n/n! * e^(-a)$ | ... |
Свойства:
1) Условие нормировки
   $\sum^\inf_k=0 a^k/k! * e^(-a) = e^(-a) * \sum^\inf_k=0 a^k/k! = e^(-a) * e^a = 1$
2) Функция распределения
   $F_\xi (x) = \sum^\inf_k=0 l(x-k) * a^k /k! e^(-a)$
3) Характеристическая фугкция
   $g_x(t) = M[e^(it\xi)] = \sum^1_k=0 e^(itk)a^k/k! * e^(-a) = e6(-a) * \sum^1_k=0 (ae^(it)^k/k! = e^-a * e^ae^(it) = e^a(e^it - 1)$
4) Числовая характеристика
   $\nu^1_\xi = 1/i * d/dt * g_\xi(t) = 1/i * e^a(e^(it)-1) * iae^it* |_t=0 = a => m_\xi = a$
   Заменчание:
   В определении закона Прассона условие a>0 оказывается избыточным т.к. мат ожидание a и k = 0,1,2,3,...,n,...
   ЗАмечание:
   Заметим, что ввиду полученных равенства (дисперсия = а) в определении ПУассона a != 0
   $\nu^2_\xi = 1/i^2 * d^2/dt^2 * g_\xi(t) = 1/i^2 ( e^a(e^(it)-1) * i^2a^2e^2it + i^2 * ae^it *e^(n(e^it - 1))) |_t=0 = a^2 + a => d_\xi = \nu^2_\xi - m^2_\xi = a^2 + a - a^2 = a$
5) Наибольшее вероятное значение $a - 1 \leq \xi^* \leq a$

Законн Прассона является предельным случаем биномиального распределения. При n-> \inf, p -> 0, np = const = 0
$$\lim C^k_n p^k (1-p)^n-k = a^k/k! * e(-a)
n->inf
p->a
np = a, p = a/n$$
$\lim_(p=a/n; n->\inf) C^k_n p^k q^(n-k) = \lim_(n->\inf) C^k_n (a/n)^k (1-a/n)^(n-k) = \lim_(n->\inf)n!/k!(n-k)! * a^k / n^k * (1-a/n)^(n-k) =....=a^k/k! * e^(-a)$
Из закона Пуассо, следует, что $\xi~П(a)$моделирует число появления а из серии nгигантского кол-ва испытаний. В этой связи закон Пуссаона получил массовое применения в сфере массового обслуживания.

Пункт 4. Геометрический закон распределения (схема до первого успеха)
Опр
Случайная величина кси распрделена по геометрчиескому закону с параметром. Че то там моделирует кол-во исптытаний до первого наступления события А

\xi ~ G(p)
p(\xi = k) = q^(k-1)p, k = 1, 2, 3, ...
p принадлежит (0, 1)

| $\xi$ | 1    | 2    | ... | n         | ..  |
| ----- | ---- | ---- | --- | --------- | --- |
| p     | q^0p | q^1p | ... | q^(n-1)*p | ... |
Свойства:
1) Условие нормировки
   $\sum^\inf_k=0 й:(л-1) = p/q * \sum^\inf_k=i q^k = p/q * p/ (1-q) = 1$
2) Функция распределения
   $F_\xi (x) = \sum^\inf_k=0 l(x-k) * q^(k-1)*p$
3) Характеристическая фугкция
   $g_x(t) = M[e^(it\xi)] = \sum^1_k=0 e^(itk)*q^(k-1)*p = p/q \sum^1_k=0 (qe^(it)^k = e^-a * e^ae^(it) = e^a(e^it - 1)$
4) Числовая характеристика
   $\nu^1_\xi = 1/i * d/dt * g_\xi(t) = 1/i * (ipe^(it) * (1 - qe^(it)) + iqe^(it) * pe^(it))/(1-qe^(it))^2 |_t=0 = 1/i * (ip^2 + ipq)/p^2 = (p+q)/p = 1/p => m_\xi = 1/p$
\nu^2_\xi = 1/i^2 * d^2/dt^2 * g_\xi(t) = сам распиши решение, тут долго писать мне= (p+2q)/p^2 = 1+q/p^2 => d_\xi = \nu^2_\xi - m^2_\xi = a/p^2
5) \x^* = 1
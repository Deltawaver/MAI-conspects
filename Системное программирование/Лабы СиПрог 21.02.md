#Системное_программирование
``` 
дата: 21.02
состояние: сырое
запись: полная
```
По каждому из предметов подготовлен репозиторий с лабами. 
Курсач выдадут сегодня, 4 варика, можно делать в группах. 

СП: 2 пака по 4 и 5 лаб соответственно 
Будет список лаб на каждую из оценок. 
Будет зачёт, также как на ФА в прошлом семе 
2 большие темы: Аллокаторы и Бдеревья 

Аллокатор - объект, который может выделять правила по требованию 
Всего 5 алгоритмов Аллокаторов: 
1) Global heap 
2) Boundary tags 
3) Sorted list
4) Buddy System
5) Red-Black tree 

B-деревья: 
1) Btree 
2) B+tree 
3) B* tree 
4) B++tree


## Аллокатор

Хранит внутри себя какой-то объем памяти
Может выдать n байт
Интерфейс:
- void do_allocate(size_t, bytes)
- void do_deallocate(void* p)
- bool do_equal(memory_resourse*)

Требования:
2) все операции должны быть безопасные

Как устроен любой из аллокаторов:![[Pasted image 20260221105714.png]]

![[Pasted image 20260221111211.png]]

![[Pasted image 20260221113142.png]]
![[Pasted image 20260221113148.png]]

## CodeTime

#### Реализация аллокатора Global Heap
*allocator_global_heap.cpp*
```C++
#include <not_implemented.h>
#include "../include/allocator_global_heap.h"

allocator_global_heap::allocator_global_heap()
{
}

[[nodiscard]] void *allocator_global_heap::do_allocate_sm(
    size_t size)
{
	return ::operator new(size);
}

void allocator_global_heap::do_deallocate_sm(
    void *at)
{
	::operator.delete(at);
}

allocator_global_heap::~allocator_global_heap()
{
}

allocator_global_heap::allocator_global_heap(const allocator_global_heap &other)
{
}

allocator_global_heap &allocator_global_heap::operator=(const allocator_global_heap &other)
{
}

bool allocator_global_heap::do_is_equal(const std::pmr::memory_resource &other) const noexcept
{
	return dynamic_cast<const allocator_global_heap *>(&other);
}

allocator_global_heap::allocator_global_heap(allocator_global_heap &&other) noexcept
{
}

allocator_global_heap &allocator_global_heap::operator=(allocator_global_heap &&other) noexcept
{
}
```